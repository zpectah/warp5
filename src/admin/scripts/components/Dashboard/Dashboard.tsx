import React from 'react';

interface DashboardProps {}

const Dashboard: React.FC<DashboardProps> = ({}) => {
	return (
		<>
			dashboard component <div>...Dashboard...</div>
		</>
	);
};

export default Dashboard;
